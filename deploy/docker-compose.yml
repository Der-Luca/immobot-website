services:
  website:
    image: nginx:1.27-alpine
    container_name: immobot-website
    restart: unless-stopped
    volumes:
      - ./:/srv/website:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - proxy-net

networks:
  proxy-net:
    external: true

